# Computer Systems & Networks

This repository contains lab work for the Computer Systems & Networks module.

Each lab is captured as:
- A Markdown write-up (`labs/`)
- Any supporting shell scripts (`scripts/`)
- Captured command output (`outputs/`)

Each lab corresponds to a single Git commit.
